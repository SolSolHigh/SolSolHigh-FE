import { tv } from 'tailwind-variants';

export const containerStyles = tv({
  base: 'w-full h-full flex flex-col justify-between items-center max-w-[30rem] mob:px-4',
});

export const titleStyles = tv({
  base: 'flex flex-col items-center gap-y-2',
});
