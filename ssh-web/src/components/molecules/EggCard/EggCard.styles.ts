import { tv } from 'tailwind-variants';

export const eggCardStyles = tv({
  base: 'flex flex-row items-center justify-between w-full h-28 p-4 rounded-3xl transition-all duration-300 cursor-pointer shadow-md hover:shadow-lg hover:bg-opacity-90 bg-secondary-100 hover:bg-secondary-200',
});
