import { tv } from 'tailwind-variants';

export const loginNavStyles = tv({
  base: 'w-full flex flex-col justify-center items-center gap-y-2 px-4 py-8 desktop:mt-4 tablet:absolute tablet:bottom-0 tablet:bg-white tablet:rounded-t-[2rem]',
});
