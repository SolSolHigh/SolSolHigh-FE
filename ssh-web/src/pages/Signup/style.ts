import { tv } from 'tailwind-variants';

export const contentStyles = tv({
  base: 'desktop:w-[48rem] desktop:h-[48rem] desktop:rounded-lg tablet:w-full tablet:h-full tablet:rounded-t-[2rem] bg-white',
});
