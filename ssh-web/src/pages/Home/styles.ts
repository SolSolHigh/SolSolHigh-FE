import { tv } from 'tailwind-variants';

export const contentStyles = tv({
  base: 'flex flex-col items-center desktop:w-[48rem] desktop:h-[48rem] desktop:rounded-lg desktop:py-16 tablet:w-full tablet:h-full tablet:py-8 mob:px-4 bg-white',
});
